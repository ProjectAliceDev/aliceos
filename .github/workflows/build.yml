name: Build AliceOS Archive
on:
  push:
    branches:
      - '*'
  release:
    types: [published]


jobs:
  strategy:
    matrix:
      renpy-versions:
        - '7.3.2'
        - '6.99.12.4'
  build:
    name: Build with Ren'Py SDK
    runs-on: ubuntu-latest
    steps:
      - name: Clone source
        uses: actions/checkout@v1
        with:
          fetch-depth: 50
      - name: Install Ren'Py SDK
        shell: bash
        run: |
          cd ..
          aliceos/travis/install.sh ${{ matrix.renpy-versions }}
      - name: Setup AliceOS for build
        shell: bash
        run: |
          cd ..
          mkdir mod
          mkdir "./mod/game"
          rm -rf aliceos/game/README.html && cp -vRf aliceos/game/* mod/game/
      - name: Add release tag to Build ID
        shell: bash
        run: |
          cd ..
          aliceos/travis/inject_version.sh
          cd ..
        if: github.event.release
        env:
          TRAVIS_TAG: ${{ github.ref }}
      - name: Add build hash to Build ID
        shell: bash
        run: |
          cd ..
          aliceos/travis/inject_version.sh
          cd ..
        if: contains(github.event_name, 'push')
        env:
          TRAVIS_COMMIT: ${{ github.sha }}
      - name: Build AliceOS archive
        shell: bash
        run: |
          cd ../renpy
          ./renpy.sh launcher distribute "../mod/"
          cd ..
        env:
          SDL_AUDIODRIVER: dummy
          SDL_VIDEODRIVER: dummy
      - name: Release artifacts to GitHub Release
        uses: JasonEtco/upload-to-release@master
        if: github.event.release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: ${{ format('AliceOS-{0}-dists/AliceOS-{1}-_ASBaseSystem.zip', '$TRAVIS_TAG', '$TRAVIS_TAG') }}